version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: trackr-mongo
    ports:
      - '27017:27017'
    volumes:
      - trackr-mongo-data:/data/db
    restart: unless-stopped

volumes:
  trackr-mongo-data:
